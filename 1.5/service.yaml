apiVersion: v1
kind: Service
metadata:
  name: service-svc
spec:
  selector:
    app: service-svc
  ports:
    - name: frontend
      protocol: TCP
      port: 9001
      targetPort: 8080
    - name: backend
      protocol: TCP
      port: 9002
      targetPort: 80
