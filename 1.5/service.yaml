apiVersion: v1
kind: Service
metadata:
  name: service-svc
spec:
  selector:
    app: service-svc
  ports:
    - name: backend
      protocol: TCP
      port: 8080
      targetPort: 8080
    - name: frontend
      protocol: TCP
      port: 8081
      targetPort: 80
