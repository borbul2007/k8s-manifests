apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appp-name  }}-frontend-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.appp-name  }}-frontend
  ports:
    - name: {{ .Values.appp-name  }}-frontend
      protocol: TCP
      port: 9001
      targetPort: 80
