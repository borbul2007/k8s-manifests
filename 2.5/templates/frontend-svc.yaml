apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app-name }}-frontend-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.app-name }}-frontend
  ports:
    - name: {{ .Values.app-name }}-frontend
      protocol: TCP
      port: 9001
      targetPort: 80
