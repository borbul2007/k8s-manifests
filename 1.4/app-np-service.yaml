apiVersion: v1
kind: Service
metadata:
  name: nginx-multitools-np-svc
spec:
  selector:
    app: nginx-multitools
  ports:
    - name: multitools-np
      protocol: TCP
      port: 9001
      targetPort: 8080
    - name: nginx-np
      protocol: TCP
      port: 9002
      targetPort: 80
