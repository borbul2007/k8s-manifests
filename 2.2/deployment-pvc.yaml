apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-logs-pvc
spec:
  storageClassName: manual
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100M